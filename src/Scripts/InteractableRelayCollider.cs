using Godot;
using System;

public partial class InteractableRelayCollider : Interactable
{
	// [Export] private NodePath interactableNodePath;
	// [Export] private NodePath itemNodePath;
    // public Interactable Interactable { get; private set; }

	// public override void _Ready()
	// {
	// 	base._Ready();
	// 	if(itemNodePath != null)
	// 	{ Interactable = GetNode<Item>(itemNodePath).Pickup; }
	// 	else
	// 	{ Interactable = GetNode<Interactable>(interactableNodePath); }
		
	// }
}
