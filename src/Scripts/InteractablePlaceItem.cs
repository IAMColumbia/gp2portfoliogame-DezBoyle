using Godot;
using System;

public partial class InteractablePlaceItem : Interactable
{
	// [Export] private NodePath hologramItemPath;
	// [Export] private NodePath placedItemPath;

	// private Node3D hologramItem;
	// private Node3D placedItem;

	// // Called when the node enters the scene tree for the first time.
	// public override void _Ready()
	// {
	// 	hologramItem = GetNode<Node3D>(hologramItemPath);
	// 	placedItem = GetNode<Node3D>(placedItemPath);
	// }

	// public override void Interact(bool once, Player player, Vector2 mouseDistance, Vector3 worldMouseDistance)
    // {
    //     if(!once)
	// 	{ return; }

	// 	//player.PlayerInventory.ResetItem();
	// 	hologramItem.Visible = false;
	// 	placedItem.Visible = true;
    // }

	// // Called every frame. 'delta' is the elapsed time since the previous frame.
	// public override void _Process(double delta)
	// {
	// }
}
